# Dependencies & Build artifacts
node_modules/
dist/
.turbo/
.tsup/

# Logs & caches
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Kapibala user settings and session stores
.kapibala/
**/.kapibala/
*.jsonl

# Local agent workspace (session memory / diagnostics), never part of the package
.workbuddy/

# Personal requirement notes (local scratch, not part of the package)
需求点.txt

# Environment variables & API Keys (CRITICAL: NEVER COMMIT)
.env
.env.*
*.env
*.env.local
*.local
*.key
*.pem
*.token
*.credentials

# OS & Editor files
.DS_Store
Thumbs.db
.idea/
